/**
 * TODO:
 * Implement this method to return a map where:
 * - Each key is a string from the list that appears at least twice.
 * - Each value is the index of the SECOND occurrence of that string.
 * - Only include each string once in the map (i.e., ignore further duplicates).
 *
 * Example 1:
 * Input: ["a", "b", "a", "c", "b"]
 * Output: {"a"=2, "b"=4}
 *
 * Example 2:
 * Input: ["x", "y", "z"]
 * Output: {}
 *
 * Example 3:
 * Input: ["cat", "dog", "cat", "cat", "dog"]
 * Output: {"cat"=2, "dog"=4}
 *
 * Notes:
 * - The "second occurrence" means the index where the element appears for the second time.
 * - Indexing is 0-based.
 * - Do not include elements that appear only once.
 */
public Map<String, Integer> mapSecondOccurrenceIndex(List<String> list) {
    
    // TODO: write your implementation here
    
}
